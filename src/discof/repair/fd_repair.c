#include "fd_repair.h"
#include "../../ballet/sha256/fd_sha256.h"

void *
fd_repair_new( void * shmem, ulong peer_max, ulong sent_max, fd_repair_sign_fn sign_fn ) {

  if( FD_UNLIKELY( !shmem ) ) {
    FD_LOG_WARNING(( "NULL mem" ));
    return NULL;
  }

  if( FD_UNLIKELY( !fd_ulong_is_aligned( (ulong)shmem, fd_repair_align() ) ) ) {
    FD_LOG_WARNING(( "misaligned mem" ));
    return NULL;
  }

  int lg_peer_max = fd_ulong_find_msb( fd_ulong_pow2_up( peer_max ) );
  int lg_sent_max  = fd_ulong_find_msb( fd_ulong_pow2_up( sent_max  ) );

  ulong footprint = fd_repair_footprint( peer_max, sent_max );
  fd_memset( shmem, 0, footprint );

  FD_SCRATCH_ALLOC_INIT( l, shmem );
  fd_repair_t * repair   = FD_SCRATCH_ALLOC_APPEND( l, fd_repair_align(),          sizeof( fd_repair_t )                       );
  void *        peer_map = FD_SCRATCH_ALLOC_APPEND( l, fd_repair_peer_map_align(), fd_repair_peer_map_footprint( lg_peer_max ) );
  void *        sent_map = FD_SCRATCH_ALLOC_APPEND( l, fd_repair_sent_map_align(), fd_repair_sent_map_footprint( lg_sent_max ) );
  FD_TEST( FD_SCRATCH_ALLOC_FINI( l, fd_repair_align() ) == (ulong)shmem + footprint );

  repair->peer_map = fd_repair_peer_map_new( peer_map, lg_peer_max );
  repair->sent_map = fd_repair_sent_map_new( sent_map, lg_sent_max );
  repair->sign_fn  = sign_fn;

  return shmem;
}

fd_repair_t *
fd_repair_join( void * shrepair ) {
  fd_repair_t * repair = (fd_repair_t *)shrepair;

  if( FD_UNLIKELY( !repair ) ) {
    FD_LOG_WARNING(( "NULL repair" ));
    return NULL;
  }

  if( FD_UNLIKELY( !fd_ulong_is_aligned((ulong)repair, fd_repair_align() ) ) ) {
    FD_LOG_WARNING(( "misaligned repair" ));
    return NULL;
  }

  fd_wksp_t * wksp = fd_wksp_containing( repair );
  if( FD_UNLIKELY( !wksp ) ) {
    FD_LOG_WARNING(( "repair must be part of a workspace" ));
    return NULL;
  }

  repair->peer_map = fd_repair_peer_map_join( repair->peer_map );
  repair->sent_map  = fd_repair_sent_map_join ( repair->sent_map  );

  return repair;
}

void *
fd_repair_leave( fd_repair_t const * repair ) {

  if( FD_UNLIKELY( !repair ) ) {
    FD_LOG_WARNING(( "NULL repair" ));
    return NULL;
  }

  return (void *)repair;
}

void *
fd_repair_delete( void * repair ) {

  if( FD_UNLIKELY( !repair ) ) {
    FD_LOG_WARNING(( "NULL repair" ));
    return NULL;
  }

  if( FD_UNLIKELY( !fd_ulong_is_aligned((ulong)repair, fd_repair_align() ) ) ) {
    FD_LOG_WARNING(( "misaligned repair" ));
    return NULL;
  }

  // TODO: zero out mem?

  return repair;
}

fd_repair_pong_t *
fd_repair_pong( fd_repair_t * repair, fd_hash_t * ping_hash ) {
  fd_repair_private_pong( &repair->identity_key, ping_hash, repair->sign_fn, &repair->pong );
  return &repair->pong;
}

/* The Pong hash is generated by concatenating the Ping hash together
    with a prefix, and hashing that.

    The Pong signature is also generated from that concatenation. */

void
fd_repair_private_pong( fd_pubkey_t *      from,
                        fd_hash_t *        ping_hash,
                        fd_repair_sign_fn  sign_fn,
                        fd_repair_pong_t * pong_out ) {
  pong_out->kind = FD_REPAIR_KIND_PONG;
  pong_out->from = *from;

  uchar preimage[ FD_REPAIR_PREIMAGE_SZ ];
  memcpy( preimage,                                FD_REPAIR_PREIMAGE_PREFIX, FD_REPAIR_PREIMAGE_PREFIX_SZ );
  memcpy( preimage + FD_REPAIR_PREIMAGE_PREFIX_SZ, ping_hash,                 sizeof(fd_hash_t)            );
  fd_sha256_hash( preimage, FD_REPAIR_PREIMAGE_SZ, pong_out->hash.uc );

  sign_fn( pong_out->hash.uc, sizeof(fd_hash_t), pong_out->sig );
}

/* hdr_req populates the common header of {Shred,HighestShred,Orphan}
   requests. */

void hdr_req( fd_pubkey_t * from, fd_pubkey_t * to, ulong ts, uint nonce, fd_repair_req_t * req_out ) {
  req_out->from  = *from;
  req_out->to    = *to;
  req_out->ts    = ts;
  req_out->nonce = nonce;
}

/* sign signs a {Shred,HighestShred,Orphan} req.  The sig field
   awkwardly sits in the middle of the serialized struct, so this copies
   the fields before kind and after sig into a temp buffer to sign.  The
   payload is sha-256 hashed before signing. */

void sign_req( fd_repair_sign_fn sign_fn, fd_repair_req_t * req_out ) {
  uchar msg[ sizeof(fd_repair_req_t) - sizeof(fd_ed25519_sig_t) ];
  FD_STORE( uint, msg, req_out->kind );
  ulong sz = fd_ulong_if( req_out->kind == FD_REPAIR_KIND_ORPHAN_REQ, sizeof(msg) - sizeof(ulong), sizeof(msg) );
  memcpy( msg + sizeof(uint), req_out->from.uc, sz - sizeof(uint) );
  sign_fn( msg, sz, req_out->sig );
}

void
fd_repair_private_shred_req( fd_pubkey_t *     from,
                             fd_pubkey_t *     to,
                             ulong             ts,
                             uint              nonce,
                             ulong             slot,
                             ulong             shred_idx,
                             fd_repair_sign_fn sign_fn,
                             fd_repair_req_t * req_out ) {
  hdr_req( from, to, ts, nonce, req_out );
  req_out->kind = FD_REPAIR_KIND_SHRED_REQ;
  req_out->shred.slot = slot;
  req_out->shred.shred_idx = shred_idx;
  sign_req( sign_fn, req_out );
}

void
fd_repair_private_highest_shred_req( fd_pubkey_t *     from,
                                     fd_pubkey_t *     to,
                                     ulong             ts,
                                     uint              nonce,
                                     ulong             slot,
                                     ulong             shred_idx,
                                     fd_repair_sign_fn sign_fn,
                                     fd_repair_req_t * req_out ) {
  hdr_req( from, to, ts, nonce, req_out );
  req_out->kind = FD_REPAIR_KIND_HIGHEST_SHRED_REQ;
  req_out->shred.slot = slot;
  req_out->shred.shred_idx = shred_idx;
  sign_req( sign_fn, req_out );
}

void
fd_repair_private_orphan_req( fd_pubkey_t *     from,
                              fd_pubkey_t *     to,
                              ulong             ts,
                              uint              nonce,
                              ulong             slot,
                              fd_repair_sign_fn sign_fn,
                              fd_repair_req_t * req_out ) {
  hdr_req( from, to, ts, nonce, req_out );
  req_out->kind = FD_REPAIR_KIND_ORPHAN_REQ;
  req_out->shred.slot = slot;
  sign_req( sign_fn, req_out );
}
