{
    "name": "block_completed",
    "id": 9,
    "description": "A block finished production, or replay on this validator, or was marked dead, or was abandoned before completing. A block gets a row if it was ever replayed or produced (had a bank), or if it finished reception on a fork that had already died or been discarded; a block dropped before reception finished gets no row. Delivery is best-effort: rows travel a lossy telemetry link, and a large burst of simultaneous rows (mass death of a fork) can drop some. On-chain data is deliberately not part of this event. Reception and repair tallies are best-effort: they restart if repair transiently stopped tracking the block (forest eviction), and on equivocated slots they are slot-scoped — shreds from all observed versions of the slot merge into the same counts on every version's row.",
    "fields": {
        "bank_seq":                                  { "type": "UInt64",        "compression": "DoubleDelta, ZSTD", "description": "Bank sequence number, counting from 1 within a run; the join key to confirmation events. Not meaningful across restarts. 0 means no bank was ever allocated (a block completing reception under a dead or discarded lineage without ever getting a bank), leaving nothing to join on bank_seq; any forward confirmations for such a block join on slot and block_id. The boot bank (bank_seq 1) emits confirmation events but no block_completed row." },
        "bank_idx":                                  { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Bank pool index the block was tracked under: the identifier log lines print. Instance-local and recycled across blocks within a run; correlate with logs only within one validator instance and time window. Reported whenever a bank existed (any row with nonzero bank_seq). The *_fork_id and cost_tracker_pool_idx columns are debug identifiers with the same scope, reported as held when the row was emitted, and 0 unless the block began replay or production (preparation_begin_time set)." },
        "txncache_fork_id":                          { "type": "UInt16",        "compression": "T64, ZSTD",         "description": "Transaction cache fork handle held by the bank. See bank_idx for scope." },
        "progcache_fork_id":                         { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Program cache fork handle held by the bank. See bank_idx for scope." },
        "accdb_fork_id":                             { "type": "UInt16",        "compression": "T64, ZSTD",         "description": "Account database fork handle held by the bank. See bank_idx for scope." },
        "vote_stakes_fork_id":                       { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Vote stakes fork handle held by the bank. See bank_idx for scope." },
        "collector_overrides_fork_id":               { "type": "UInt16",        "compression": "T64, ZSTD",         "description": "Collector overrides fork handle held by the bank. See bank_idx for scope." },
        "stake_rewards_fork_id":                     { "type": "UInt8",         "compression": "T64, ZSTD",         "description": "Stake rewards fork handle held by the bank. See bank_idx for scope." },
        "epoch_credits_fork_id":                     { "type": "UInt8",         "compression": "T64, ZSTD",         "description": "Epoch credits fork handle held by the bank. See bank_idx for scope." },
        "stake_delegations_fork_id":                 { "type": "UInt16",        "compression": "T64, ZSTD",         "description": "Stake delegations fork handle held by the bank. See bank_idx for scope." },
        "cost_tracker_pool_idx":                     { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Cost tracker pool index the bank held: snapshotted just before the freeze releases it on completed rows, read live on dead and abandoned rows; 0 if the block never began replay or production. See bank_idx for scope." },
        "slot":                                      { "type": "UInt64",        "compression": "DoubleDelta, ZSTD", "description": "Slot number of the block." },
        "epoch":                                     { "type": "UInt64",        "compression": "DoubleDelta, ZSTD", "description": "Epoch containing the slot." },
        "parent_slot":                               { "type": "UInt64",        "compression": "DoubleDelta, ZSTD", "description": "Slot number of the parent block. 0 if the parent was no longer locally known when the row was emitted; on genesis-booted clusters, 0 is also the genuine parent slot of blocks chaining directly off genesis." },
        "root_slot":                                 { "type": "UInt64",        "compression": "DoubleDelta, ZSTD", "description": "Finalized root slot when the row was emitted." },
        "storage_slot":                              { "type": "UInt64",        "compression": "DoubleDelta, ZSTD", "description": "Lowest slot still held in memory when the row was emitted." },
        "turbine_slot":                              { "type": "UInt64",        "compression": "DoubleDelta, ZSTD", "description": "Highest slot observed in completed FEC sets from the network (own leader blocks excluded) when the row was emitted: the validator's estimate of the cluster tip. Minus slot: how far behind this row was - 0 to 2 in steady state, large during boot catch-up and while racing back after any stall, re-detecting mid-run lag that caught_up cannot. 0 before any shred has been observed." },
        "caught_up":                                 { "type": "Bool",                                              "description": "Whether the validator considered itself caught up to the cluster when the row was emitted. False from boot until replay closes on the network tip (set immediately for genesis bootstrap); never cleared within a run." },
        "fork_width":                                { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Number of forks executing concurrently when the row was emitted. 1 in calm operation; higher values mark fork storms, explaining replay contention, evictions, and prune bursts on the rows that suffered them." },
        "snapshot_in_progress":                      { "type": "Bool",                                              "description": "Whether this validator was writing a snapshot when the row was emitted; snapshot creation periodically stalls replay, so timing outliers on these rows are expected. Always false when snapshot creation is unsupported or disabled." },
        "live_bank_count":                           { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Banks allocated when the row was emitted: executing, awaiting replay, and completed or dead awaiting prune. Bank tracking pressure; blocks are evicted when this reaches the pool bound. fork_width isolates the concurrently-executing component." },
        "block_id":                                  { "type": "Hash",                                              "description": "Block identifier. For blocks that never completed reception (dead or abandoned early): the merkle root of the latest piece received." },
        "parent_block_id":                           { "type": "Hash",                                              "description": "Block id of the parent this block chained from, recovered from the reception chain; disambiguates the parent version when the parent slot equivocated. 0 if the reception chain could not be walked to the parent: reassembly no longer tracked the block, or an abandoned leader block that shredded nothing; parent_slot can still be known on such rows." },
        "bank_hash":                                 { "type": "Hash",                                              "description": "Bank hash computed by replaying the block; the state commitment consensus votes on. Rows sharing slot and block_id but differing here indicate state divergence. 0 if the block never froze (dead or abandoned)." },
        "dead":                                      { "type": "Bool",                                              "description": "Whether the block was ruled invalid. Blocks merely given up on are not dead; see abandoned_reason. State accumulated before the ruling is still reported, so execution fields may be partially filled." },
        "dead_reason": {
            "type": "LowCardinality(String)",
            "description": "Why the block was ruled invalid; not_dead otherwise. Blocks this validator produced are never dead: they do not go through replay's block checks. A block invalid for several independent reasons records the first one detected locally, which can differ across validators for the same block.",
            "variants": {
                "not_dead":                    { "description": "Not ruled invalid." },
                "parent_dead":                 { "description": "The block went down with its lineage: an ancestor was ruled invalid, so this block cannot be replayed." },
                "unparseable_content":         { "description": "Bytes at the head of the stream failed to parse as any structure (transaction, microblock header, or count) within the largest size a valid block allows: malformed content." },
                "short_block":                 { "description": "Block bytes ended short of the microblocks and transactions declared." },
                "trailing_entry":              { "description": "Block did not end on a tick; a transaction entry trailed the final tick. Checked before tick-count verification, so a block Agave rules TooFewTicks can report trailing_entry here." },
                "too_many_microblocks":        { "description": "More microblocks than a valid block can hold." },
                "tick_hashes_overflow":        { "description": "More hashes since the last tick than hashes per tick allows, detected at tick verification." },
                "tick_hashes_overflow_ingest": { "description": "A microblock header declared more hashes than fit before the next tick, detected at FEC ingest." },
                "inconsistent_tick_hashes":    { "description": "A tick's hash count differs from the block's preceding ticks, detected at FEC ingest." },
                "zero_hash_tick":              { "description": "A tick advanced zero hashes, detected at tick verification because proof-of-history parameters were unknown when the tick parsed." },
                "zero_hash_tick_ingest":       { "description": "A tick advanced zero hashes, detected at FEC ingest." },
                "zero_microblocks":            { "description": "A batch header declared zero microblocks." },
                "too_many_txns":               { "description": "More transactions than a valid block can hold." },
                "too_many_ticks":              { "description": "More ticks than allowed." },
                "wrong_hashes_per_tick":       { "description": "Tick hash count did not advance the expected hashes per tick." },
                "too_few_ticks":               { "description": "Fewer ticks than required." },
                "tick_hash_mismatch":          { "description": "Proof-of-history hash of a tick did not verify." },
                "entry_hash_mismatch":         { "description": "Proof-of-history hash of a transaction entry did not verify, detected when the entry's hashing task completed." },
                "entry_hash_mismatch_ingest":  { "description": "Proof-of-history hash of a transaction entry did not verify, detected at FEC ingest when a later FEC set completed the entry's transactions." },
                "sigverify_failed":            { "description": "A transaction failed signature verification." },
                "txn_failed_to_load":          { "description": "A transaction failed a pre-execution check: already processed, expired or unknown recent blockhash, address lookup table resolution failure, account lock limit, compute budget error, unsupported transaction version or sanitization failure, or fee-payer validation failure." },
                "block_cost_limit":            { "description": "A transaction pushed the block over its compute limit." },
                "account_cost_limit":          { "description": "A transaction pushed one account over its per-block compute limit." },
                "vote_cost_limit":             { "description": "A vote transaction pushed the block over its vote compute limit." },
                "account_data_limit":          { "description": "A transaction pushed the block over its account data size limit." },
                "duplicate_account":           { "description": "A transaction referenced the same account more than once." }
            }
        },
        "dead_time":                                 { "type": "DateTime64(9)",                                     "description": "When the block was ruled invalid; 0 if not dead." },
        "abandoned":                                 { "type": "Bool",                                              "description": "Whether this validator gave up on the block before it completed. Independent of dead: an abandoned block was not invalid. Equivalent to abandoned_reason != not_abandoned. State accumulated before abandonment is still reported, so execution fields may be partially filled." },
        "abandoned_reason": {
            "type": "LowCardinality(String)",
            "description": "Why this validator gave up on the block before it completed; not_abandoned otherwise. Independent of dead.",
            "variants": {
                "not_abandoned": { "description": "Not abandoned; the block completed or was ruled invalid." },
                "pruned":        { "description": "A block on another fork was rooted, discarding this fork before replay finished." },
                "evicted":       { "description": "Dropped for lack of room to track in-progress blocks. The row is emitted once the block's in-flight verification has drained without ruling it invalid; a block ruled invalid while draining emits a dead row instead. May be replayed later, producing a second row for the same slot and block id under a new bank_seq; the retry usually completes but can itself end dead, evicted, or pruned. Blocks evicted after freezing already emitted a completed row and emit nothing further. Dedup on slot and block_id, keeping the highest bank_seq." },
                "reset":         { "description": "Production of this leader block was abandoned mid-slot: consensus reset block production onto another fork before the slot completed. Only on blocks this validator produced; pack_end_reason is abandoned on the same row. The partial block never finished shredding, so block_id holds the merkle root of the last piece that had returned through reassembly when production was abandoned (may trail the last piece actually shredded), or 0 if none had." }
            }
        },
        "abandoned_time":                            { "type": "DateTime64(9)",                                     "description": "When the block was abandoned; 0 if not abandoned." },
        "is_leader":                                 { "type": "Bool",                                              "description": "Whether this validator produced the block as leader." },
        "first_shred_received_time":                 { "type": "DateTime64(9)", "compression": "DoubleDelta, ZSTD", "description": "Network arrival at the shred tile of the block's first shred, stamped before signature verification. Leader blocks: when the first entry batch reached the shredding stage, before any network send. 0 if repair pruned the block before completion." },
        "last_shred_received_time":                  { "type": "DateTime64(9)", "compression": "DoubleDelta, ZSTD", "description": "Network arrival of the shred that made the block contiguous; not necessarily the highest-numbered shred. Minus first_shred_received_time: reception duration, valid only when this is nonzero. Leader blocks: when the final entry batch reached the shredding stage. 0 if the block never became fully contiguous (dead or abandoned mid-reception) or repair pruned it before completion." },
        "first_repair_request_time":                 { "type": "DateTime64(9)",                                     "description": "First repair request for a specific missing shred (window request); highest-shred and orphan requests do not stamp this, so it can be 0 while their request counts are not. 0 if no window request was sent or repair pruned the block; always 0 for leader blocks." },
        "last_repair_received_time":                 { "type": "DateTime64(9)",                                     "description": "Arrival of the last repair response matched to an outstanding specific-shred request; shreds from highest-shred and orphan responses do not stamp this. 0 if none was matched or repair pruned the block; always 0 for leader blocks." },
        "first_fec_set_received_time":               { "type": "DateTime64(9)", "compression": "DoubleDelta, ZSTD", "description": "When the block's first complete FEC set reached replay, after verification and reassembly. Minus first_shred_received_time: internal pipeline latency from network to replay, valid only when both are nonzero. Leader blocks: when the block's own first FEC set returned to replay through reassembly; follows rather than precedes preparation_begin_time, since production shreds the block as it goes. 0 if no bank was ever allocated, and on abandoned leader blocks whose first FEC set had not returned through reassembly when production was abandoned." },
        "preparation_begin_time":                    { "type": "DateTime64(9)", "compression": "DoubleDelta, ZSTD", "description": "When replay began setting up the block's execution state: cloning from the parent and applying start-of-block updates (epoch boundary processing, fee and rent state). Follows first_fec_set_received_time on replayed blocks; leader blocks reverse that order (see that field). Gap to first_transaction_scheduled_time: setup cost, which for leader blocks includes waiting for pack to schedule the first transaction; valid only when first_transaction_scheduled_time is nonzero. 0 if the block was dropped before setup." },
        "first_transaction_scheduled_time":          { "type": "DateTime64(9)", "compression": "DoubleDelta, ZSTD", "description": "When replay dispatched the block's first transaction to an execution tile. Leader blocks: when an execution tile began the first packed transaction. Execution and verification run from here to last_transaction_finished_time; equal to it if the block executed no transactions or, for leader blocks, if production timing was unavailable (see txn_timing). 0 on dead and abandoned rows if no transaction was ever dispatched." },
        "last_transaction_finished_time":            { "type": "DateTime64(9)", "compression": "DoubleDelta, ZSTD", "description": "When the block became fully executed and verified and end-of-block processing began: all execution and signature-verification results returned, the final FEC set received, PoH verified. At the live tip this trails the last execution result by trailing-tick reception and verification; per-transaction execution end is txn_timing.replayed_time. Leader blocks: when the block was fully produced (final PoH hash received, block id returned through reassembly), trailing the last execution result by the PoH tail and final-entry shredding, most visibly when a limit ended the block early. 0 if the block never finished executing (dead or abandoned)." },
        "block_completed_time":                      { "type": "DateTime64(9)", "compression": "DoubleDelta, ZSTD", "description": "When end-of-block processing finished and the block froze: state hashing complete, no further changes. Minus last_transaction_finished_time: finalization cost. 0 if the block never completed (dead or abandoned)." },
        "parent_block_completed_time":               { "type": "DateTime64(9)", "compression": "DoubleDelta, ZSTD", "description": "The parent's block_completed_time, denormalized to avoid a self-join; for blocks chaining directly off the snapshot or genesis boot bank, when the boot bank finished loading. 0 if the parent's completion was never observed: parent untracked, or parent never froze (dead or abandoned lineage). Slot-time queries (block_completed_time minus this) should filter on caught_up, parent_slot = slot - 1, and a nonzero value here." },
        "fec_set_count":                             { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Number of FEC sets in the block; for blocks that never completed reception, the number delivered to replay when the row was emitted. 0 if reassembly no longer tracked the block when the row was emitted." },
        "data_shred_count":                          { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "Data shreds received or recovered. Leader blocks: shreds produced. 0 if repair pruned the block before completion." },
        "parity_shred_count":                        { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "Parity (recovery) shreds received. Near 0 for leader blocks: only network echoes of the block's own parity shreds land here. 0 if repair pruned the block before completion." },
        "turbine_shred_count":                       { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "Shreds received over the network via turbine, plus repair deliveries whose provenance could not be verified (failed nonce) and coding shreds arriving in repair responses (nonconformant: repair serves only data shreds). Pieces cleared and re-fetched after a wrong-version detection or eviction count once per delivery, so source tallies can exceed the block's shred counts. Near 0 for leader blocks. 0 if repair pruned the block before completion." },
        "repair_shred_count":                        { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "Shreds obtained from peers via repair; nonce-verified deliveries only. Re-fetched pieces count once per delivery (see turbine_shred_count). Always 0 for leader blocks. 0 if repair pruned the block before completion." },
        "recovered_shred_count":                     { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "Shreds reconstructed locally from parity data. Re-recovered pieces count once per recovery (see turbine_shred_count). Always 0 for leader blocks, which produce rather than recover their shreds. 0 if repair pruned the block before completion." },
        "chain_confirmed":                           { "type": "Bool",                                              "description": "Whether every FEC set was verified to chain back to the confirmed block identity, i.e. the assembled data matches the block the cluster confirmed. Verification is confirmation-triggered and the value is snapshotted at the block's last received FEC set, so false means it had not run by then, not that it failed; at the live tip confirmation usually arrives later. True mainly when confirmation precedes reception completing, e.g. during catch-up. See lowest_verified_fec_index." },
        "lowest_verified_fec_index":                 { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "Lowest FEC set index reached by chain verification as of the block's last received FEC set; the walk anchors at the confirmed block identity and works backwards. 0 means the whole block verified. 4294967295 means no verification information (verification never ran, or repair pruned the block); not a FEC index." },
        "last_completed_fec_set_index":              { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "FEC set index of the last piece of the block to complete reception, which with out-of-order repair need not be the block's final index. 4294967295 when the reception statistics are unpopulated; not a FEC index." },
        "slot_complete_flag":                        { "type": "Bool",                                              "description": "Whether the shred marking the block complete was received. False if repair pruned the block before completion." },
        "equivocation_detected_shred":               { "type": "Bool",                                              "description": "Whether any of the block's FEC sets was ever quarantined by an equivocation hold: set when a conflicting version of a FEC set was assembled at that position, and inherited by everything chaining onto or arriving during an unresolved conflict, including from ancestor slots; persists after resolution. A lone conflicting shred discarded before reassembly does not register. False if reassembly no longer tracked the block when the row was emitted." },
        "repair_requests_retransmitted":             { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "Repair requests re-sent after a response timeout. Always 0 for leader blocks. 0 if repair pruned the block before completion." },
        "repair_responses_received":                 { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "Repair responses matched to an outstanding specific-shred request. Shreds delivered by verified highest-shred and orphan responses count in repair_shred_count but are not matched here, so this can be 0 while repair_shred_count is not. Always 0 for leader blocks. 0 if repair pruned the block before completion." },
        "repair_request_window_count":               { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "Repair requests for one specific missing shred, by index. Always 0 for leader blocks. 0 if repair pruned the block before completion." },
        "repair_request_highest_window_count":       { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "Repair requests for the highest-numbered shred a peer holds, used to learn the block's length. Highest-shred requests seeded at boot, before the block was tracked, bypass this counter. Always 0 for leader blocks. 0 if repair pruned the block before completion." },
        "repair_request_orphan_count":               { "type": "UInt32",        "compression": "T64, ZSTD",         "description": "Repair requests for the block's ancestry when its parent is unknown; a response carries shreds from up to 11 ancestor blocks. Always 0 for leader blocks. 0 if repair pruned the block before completion." },
        "repair_failed_chain_verify":                { "type": "Bool",                                              "description": "Whether chain verification against the confirmed block identity found a mismatched (wrong-version) FEC held for this block; the piece was cleared and re-repaired, but may have arrived by any route, turbine included. Always false for leader blocks. False if repair pruned the block before completion." },
        "cost_tracker_block_cost":                   { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Compute units used by all transactions, votes included; the vote/non-vote split is not part of this event (pack_vote_cost carries the producer-side vote reservation on leader rows). 0 if no cost tracker existed for the block (never prepared for replay, or no bank)." },
        "cost_tracker_allocated_accounts_data_size": { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "New account data allocated, in bytes. 0 if no cost tracker existed for the block." },
        "cost_tracker_block_cost_limit":             { "type": "UInt64",                                            "description": "Block compute unit limit. 0 if no cost tracker existed for the block (never prepared for replay, or no bank)." },
        "cost_tracker_vote_cost_limit":              { "type": "UInt64",                                            "description": "Block vote compute unit limit. 0 if no cost tracker existed for the block." },
        "cost_tracker_account_cost_limit":           { "type": "UInt64",                                            "description": "Per-account compute unit limit. 0 if no cost tracker existed for the block." },
        "became_leader_time":                        { "type": "DateTime64(9)",                                     "description": "When replay began block production for the slot, all prerequisites (e.g. parent block identity) in hand. Minus leader_slot_start_time: local production start delay. 0 for blocks this validator did not produce." },
        "leader_slot_start_time":                    { "type": "DateTime64(9)",                                     "description": "When the leader slot was scheduled to begin, estimated locally by projecting slot durations from the reset onto the parent fork; embeds upstream lateness rather than tracking a cluster-absolute timeline. 0 for blocks this validator did not produce." },
        "pack_start_time":                           { "type": "DateTime64(9)",                                     "description": "When block production began. 0 for blocks this validator did not produce." },
        "pack_end_time":                             { "type": "DateTime64(9)",                                     "description": "When block production ended. 0 for blocks this validator did not produce." },
        "microblock_count":                          { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Microblocks pack scheduled during production. A microblock whose transactions all failed execution is skipped at PoH mixin and absent from the block, so this can exceed block content. 0 for blocks this validator did not produce." },
        "pack_block_cost":                           { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Compute units pack reserved while building the block, votes included. Reservations are refunded as transactions finish, so this can exceed cost_tracker_block_cost (the charged amount) by refunds unsettled at slot end. 0 for blocks this validator did not produce." },
        "pack_vote_cost":                            { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Vote compute units pack reserved while building the block, less refunds settled by slot end. Pack's vote costing is not feature-gated by remove_simple_vote_from_cost_model, so this stays populated on leader rows. 0 for blocks this validator did not produce." },
        "pack_data_bytes":                           { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Bytes pack's cost model reserved for block content: transaction payloads plus a 48-byte header per microblock, net of rebates settled by slot end. Excludes tick entries; can exceed the block's actual content by rebates unsettled at slot end. A size, not a compute measure. 0 for blocks this validator did not produce." },
        "pack_end_reason": {
            "type": "LowCardinality(String)",
            "description": "Why block production stopped; not_leader for blocks this validator did not produce.",
            "variants": {
                "not_leader":       { "description": "Not produced by this validator." },
                "time":             { "description": "The leader slot's time window ended, the normal case. Blocks that filled a consensus limit also end here; measure saturation via pack_block_cost and pack_data_bytes against the protocol caps." },
                "microblock_limit": { "description": "Microblock count limit reached." },
                "abandoned":        { "description": "Production was abandoned mid-slot by a reset onto another fork; only on abandoned leader blocks (abandoned_reason reset). Pack ends the block early and its summary rides on the terminating message, so microblock_count, pack costs, pack timings, and bundle_txn_count reflect what was produced before the abandonment." }
            }
        },
        "bundle_txn_count":                          { "type": "UInt64",        "compression": "T64, ZSTD",         "description": "Bundled transactions pack scheduled during production, counted whether or not the bundle survived to the block (aborted bundles are skipped at PoH mixin). 0 for blocks this validator did not produce." },
        "txn_timing": {
            "type": "Array",
            "description": "Per-transaction timestamps, one entry per transaction in block order. Replayed blocks: stamped by the replay scheduler. Leader blocks: stamped by the production pipeline. Columns exclusive to the other path are 0. Empty on dead and abandoned replay rows; an abandoned leader block reports the transactions mixed into the block before production was abandoned. Otherwise best-effort: empty when no capture slot was free (more than 16 blocks replaying at once) or the production timing table was unavailable. Empty does not mean the block had no transactions.",
            "max_len": 98039,
            "dynamic": true,
            "element": {
                "type": "Tuple",
                "description": "Timestamps for one transaction. A zero timestamp means the stage did not occur on this path, was never reached, or its stamp was unavailable.",
                "fields": {
                    "received_time":             { "type": "DateTime64(9)", "compression": "Delta, ZSTD", "description": "Earliest time attributable to the transaction. Replayed blocks: network arrival of the shred completing the FEC set carrying the transaction; gap to parsed_time is the internal latency to parse. Leader blocks: entry into this validator — TPU network arrival, block engine receipt (bundles), gossip receipt (votes), or creation time (self-originated)." },
                    "parsed_time":               { "type": "DateTime64(9)", "compression": "Delta, ZSTD", "description": "When the scheduler parsed the transaction out of received data, including address lookup table resolution. Gap from received_time covers the pipeline hop, in-order delivery waits, and parse position within the FEC set. 0 for leader blocks." },
                    "dispatched_time":           { "type": "DateTime64(9)", "compression": "Delta, ZSTD", "description": "Replayed blocks: when the scheduler handed the transaction to an execution tile. Leader blocks: when an execution tile began executing it; transactions of one bundle share the stamp." },
                    "replayed_time":             { "type": "DateTime64(9)", "compression": "Delta, ZSTD", "description": "When execution finished and results were committed to the block's state, on whichever path executed it." },
                    "sigverify_dispatched_time": { "type": "DateTime64(9)", "compression": "Delta, ZSTD", "description": "When the scheduler dispatched signature verification, which runs in parallel with execution. 0 for leader blocks (verified upstream of pack)." },
                    "sigverify_done_time":       { "type": "DateTime64(9)", "compression": "Delta, ZSTD", "description": "When the signature verification result returned to the scheduler. 0 for leader blocks." },
                    "poh_mixed_time":            { "type": "DateTime64(9)", "compression": "Delta, ZSTD", "description": "When the transaction was mixed into the PoH stream, fixing its position in the block; transactions of one microblock share the stamp. 0 for blocks this validator did not produce." }
                }
            }
        }
    }
}
