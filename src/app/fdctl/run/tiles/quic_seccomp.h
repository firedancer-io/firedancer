#ifndef HEADER_fd_src_util_sandbox_quic_seccomp.h_h
#define HEADER_fd_src_util_sandbox_quic_seccomp.h_h

#include <linux/filter.h>

/* THIS FILE WAS GENERATED BY generate_filters.py. */
/* DO NOT EDIT BY HAND!                            */

static const unsigned int sock_filter_policy_quic_instr_cnt = 31;

static void populate_sock_filter_policy_quic(struct sock_filter (*out) [static 31]) {
  *out = {
    /* Check: Jump to RET_KILL_PROCESS if the script's arch != the runtime arch */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, ( offsetof( struct seccomp_data, arch ) ) ),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, ARCH_NR, 0, /* RET_KILL_PROCESS */ 27 ),
    /* loading syscall number in accumulator as it might have been evicted by the previous evaluation */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, ( offsetof( struct seccomp_data, nr ) ) ),
    /* begin write: (Symbol(or), (Symbol(eq), (Symbol(arg), 0), 2), (Symbol(eq), (Symbol(arg), 0), 3)) */
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, __NR_write, 0, /* lbl_1 */ 4 ),
    /* load syscall argument 0 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[0])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, 2, /* RET_ALLOW */ 24, /* lbl_2 */ 0 ),
    /* lbl_2: */
    /* load syscall argument 0 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[0])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, 3, /* RET_ALLOW */ 22, /* RET_KILL_PROCESS */ 21 ),
    /* lbl_1: */
    /* loading syscall number in accumulator as it might have been evicted by the previous evaluation */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, ( offsetof( struct seccomp_data, nr ) ) ),
    /* begin fsync: (Symbol(eq), (Symbol(arg), 0), 3) */
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, __NR_fsync, 0, /* lbl_3 */ 2 ),
    /* load syscall argument 0 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[0])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, 3, /* RET_ALLOW */ 18, /* RET_KILL_PROCESS */ 17 ),
    /* lbl_3: */
    /* loading syscall number in accumulator as it might have been evicted by the previous evaluation */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, ( offsetof( struct seccomp_data, nr ) ) ),
    /* simply allow getpid */
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, __NR_getpid, /* RET_ALLOW */ 16, 0 ),
    /* simply allow getrandom */
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, __NR_getrandom, /* RET_ALLOW */ 15, 0 ),
    /* loading syscall number in accumulator as it might have been evicted by the previous evaluation */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, ( offsetof( struct seccomp_data, nr ) ) ),
    /* begin madvise: (Symbol(eq), (Symbol(arg), 2), Symbol(MADV_DONTNEED)) */
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, __NR_madvise, 0, /* lbl_4 */ 2 ),
    /* load syscall argument 2 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[2])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, MADV_DONTNEED, /* RET_ALLOW */ 11, /* RET_KILL_PROCESS */ 10 ),
    /* lbl_4: */
    /* loading syscall number in accumulator as it might have been evicted by the previous evaluation */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, ( offsetof( struct seccomp_data, nr ) ) ),
    /* begin mmap: (Symbol(and), (Symbol(eq), (Symbol(arg), 2), Symbol(PROT_WRITE)), (Symbol(eq), (Symbol(arg), 3), 'MAP_PRIVATE | MAP_ANONYMOUS'), (Symbol(eq), (Symbol(arg), 4), -1), (Symbol(eq), (Symbol(arg), 5), 0)) */
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, __NR_mmap, 0, /* lbl_5 */ 8 ),
    /* load syscall argument 2 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[2])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, PROT_WRITE, /* lbl_6 */ 0, /* RET_KILL_PROCESS */ 6 ),
    /* lbl_6: */
    /* load syscall argument 3 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[3])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, MAP_PRIVATE | MAP_ANONYMOUS, /* lbl_7 */ 0, /* RET_KILL_PROCESS */ 4 ),
    /* lbl_7: */
    /* load syscall argument 4 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[4])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, -1, /* lbl_8 */ 0, /* RET_KILL_PROCESS */ 2 ),
    /* lbl_8: */
    /* load syscall argument 5 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[5])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, 0, /* RET_ALLOW */ 1, /* RET_KILL_PROCESS */ 0 ),
    /* lbl_5: */
    /* RET_KILL_PROCESS: */
    /* KILL_PROCESS is placed before ALLOW since it's the fallthrough case. */
    BPF_STMT( BPF_RET | BPF_K, SECCOMP_RET_KILL_PROCESS ),
    /* RET_ALLOW: */
    /* ALLOW has to be reached by jumping */
    BPF_STMT( BPF_RET | BPF_K, SECCOMP_RET_ALLOW ),
  };
}

#endif
