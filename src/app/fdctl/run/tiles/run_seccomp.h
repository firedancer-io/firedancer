#ifndef HEADER_fd_src_util_sandbox_run_seccomp.h_h
#define HEADER_fd_src_util_sandbox_run_seccomp.h_h

#include <linux/filter.h>

/* THIS FILE WAS GENERATED BY generate_filters.py. */
/* DO NOT EDIT BY HAND!                            */

static const unsigned int sock_filter_policy_run_instr_cnt = 20;

static void populate_sock_filter_policy_run(struct sock_filter (*out) [static 20], int pid_namespace) {
  *out = {
    /* Check: Jump to RET_KILL_PROCESS if the script's arch != the runtime arch */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, ( offsetof( struct seccomp_data, arch ) ) ),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, ARCH_NR, 0, /* RET_KILL_PROCESS */ 16 ),
    /* loading syscall number in accumulator as it might have been evicted by the previous evaluation */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, ( offsetof( struct seccomp_data, nr ) ) ),
    /* simply allow write (eq (arg 0) 2) */
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, __NR_write (eq (arg 0) 2), /* RET_ALLOW */ 15, 0 ),
    /* loading syscall number in accumulator as it might have been evicted by the previous evaluation */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, ( offsetof( struct seccomp_data, nr ) ) ),
    /* begin wait4: (Symbol(and), (Symbol(eq), (Symbol(arg), 0), Symbol(pid_namespace)), (Symbol(eq), (Symbol(arg), 2), Symbol(__WCLONE)), (Symbol(eq), (Symbol(arg), 3), 0)) */
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, __NR_wait4, 0, /* lbl_1 */ 6 ),
    /* load syscall argument 0 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[0])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, pid_namespace, /* lbl_2 */ 0, /* RET_KILL_PROCESS */ 10 ),
    /* lbl_2: */
    /* load syscall argument 2 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[2])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, __WCLONE, /* lbl_3 */ 0, /* RET_KILL_PROCESS */ 8 ),
    /* lbl_3: */
    /* load syscall argument 3 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[3])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, 0, /* RET_ALLOW */ 7, /* RET_KILL_PROCESS */ 6 ),
    /* lbl_1: */
    /* loading syscall number in accumulator as it might have been evicted by the previous evaluation */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, ( offsetof( struct seccomp_data, nr ) ) ),
    /* simply allow exit_group */
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, __NR_exit_group, /* RET_ALLOW */ 5, 0 ),
    /* loading syscall number in accumulator as it might have been evicted by the previous evaluation */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, ( offsetof( struct seccomp_data, nr ) ) ),
    /* begin kill: (Symbol(eq), (Symbol(arg), 1), Symbol(SIGKILL)) */
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, __NR_kill, 0, /* lbl_4 */ 2 ),
    /* load syscall argument 1 in accumulator */
    BPF_STMT( BPF_LD | BPF_W | BPF_ABS, offsetof(struct seccomp_data, args[1])),
    BPF_JUMP( BPF_JMP | BPF_JEQ | BPF_K, SIGKILL, /* RET_ALLOW */ 1, /* RET_KILL_PROCESS */ 0 ),
    /* lbl_4: */
    /* RET_KILL_PROCESS: */
    /* KILL_PROCESS is placed before ALLOW since it's the fallthrough case. */
    BPF_STMT( BPF_RET | BPF_K, SECCOMP_RET_KILL_PROCESS ),
    /* RET_ALLOW: */
    /* ALLOW has to be reached by jumping */
    BPF_STMT( BPF_RET | BPF_K, SECCOMP_RET_ALLOW ),
  };
}

#endif
