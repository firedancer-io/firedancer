#!/bin/bash

export PKG_CONFIG_LIBDIR=""
export PKG_CONFIG_PATH="$(pwd)/opt/lib/pkgconfig:$(pwd)/opt/lib64/pkgconfig"

if command -v module; then
  # Jump internal use
  module purge
  module load gcc-9.3.0  || echo "module load gcc-9.3.0 failed"
  module list
fi
