name: On Main Push
on:
  push:
    branches: [main]
jobs:
  book:
    uses: ./.github/workflows/book.yml
    secrets: inherit

  clusterfuzz:
    uses: ./.github/workflows/clusterfuzz.yml
    secrets: inherit
